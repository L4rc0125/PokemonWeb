services:
  - type: web
    name: pokemonweb
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: POKEMONWEB_PUBLIC
        value: "1"
      - key: POKEMONWEB_SESSION_SECRET
        generateValue: true
